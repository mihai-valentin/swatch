{
  "id": "swatch-parser",
  "projectId": "swatch",
  "title": "Implement hex parser + unit tests",
  "description": "Implement `swatch_parse_hex` as declared in `src/parse.h` (produced by swatch-scaffold). This is a pure function with no I/O, no globals, no side effects outside the passed-in out pointer — the easiest possible target for unit testing and sanitizer coverage.\n\nThis task DEPENDS on swatch-scaffold being complete: parse.h, tests/test.h, and the Makefile `test` target must exist. Do NOT modify parse.h — the signature and contract are fixed.\n\nRun in parallel with swatch-renderer; the two tasks share no source files.",
  "requirements": [
    "Implement `src/parse.c`:\n  - `#include \"parse.h\"` and `<ctype.h>`, `<string.h>`.\n  - Function `int swatch_parse_hex(const char *input, rgb_t *out)` matching the header contract.\n  - Return -1 immediately if `input == NULL` or `out == NULL`.\n  - Skip a single leading `#` if present.\n  - After optional `#`, the remainder must be exactly 3 or 6 hex characters (no other lengths, no embedded `#`, no whitespace).\n  - Reject on any non-hex character via `isxdigit((unsigned char)c)`. Use the `(unsigned char)` cast — isxdigit's behavior is undefined on signed char values outside [0, UCHAR_MAX].\n  - For 3-char form, expand each nibble by duplication: `'A'` → byte `0xAA`. For 6-char form, consume two hex digits per channel.\n  - Parse hex digits manually (do NOT use `strtol` / `sscanf` — both complicate error handling for this contract). A small `hex_nibble(char) → int` static helper returning -1 on non-hex is the cleanest shape.\n  - Only assign to `*out` AFTER all validation has passed. On failure, `*out` must be untouched.",
    "Implement `tests/test_parse.c`:\n  - `#include \"../src/parse.h\"` and `#include \"test.h\"`.\n  - Test functions (each `static void name(void)`):\n    * `test_six_char_with_hash` — `#00AF4C` → r=0x00 g=0xAF b=0x4C, return 0.\n    * `test_six_char_without_hash` — `FF00FF` → r=0xFF g=0x00 b=0xFF, return 0.\n    * `test_three_char_with_hash` — `#0AF` → r=0x00 g=0xAA b=0xFF, return 0.\n    * `test_three_char_without_hash` — `abc` → r=0xAA g=0xBB b=0xCC (lowercase accepted), return 0.\n    * `test_uppercase_and_lowercase_mix` — `#aAfF00` → r=0xAA g=0xFF b=0x00, return 0.\n    * `test_reject_wrong_length` — `#12`, `#1234`, `#1234567` all return -1.\n    * `test_reject_non_hex` — `#ZZZZZZ`, `#GGHHII`, `#00AF4G` all return -1.\n    * `test_reject_null_input` — `swatch_parse_hex(NULL, &rgb)` returns -1.\n    * `test_reject_null_output` — `swatch_parse_hex(\"#000000\", NULL)` returns -1.\n    * `test_reject_embedded_hash` — `00#F4C` returns -1.\n    * `test_reject_whitespace` — `\" #00AF4C\"`, `\"#00AF4C \"`, `\"00 AF4C\"` all return -1.\n    * `test_out_untouched_on_failure` — initialize out to known sentinel values, call with invalid input, assert all three channels still hold sentinels.\n  - `int main(void)` invokes each via `RUN_TEST(...)` and ends with `TEST_SUMMARY()`.",
    "Verify: `make test` exits 0 with ASan + UBSan enabled. `make clean && make` (release build) also exits 0 — proves parse.c compiles warning-free under `-Werror` in release mode too. Report the output of both."
  ],
  "allowedPaths": [
    "src/parse.c",
    "tests/test_parse.c"
  ],
  "forbiddenPaths": [
    "src/parse.h",
    "src/render.h",
    "src/render.c",
    "src/main.c",
    "tests/test.h",
    "tests/test_render.c",
    "Makefile",
    "README.md",
    "idea.md",
    "CLAUDE.md",
    ".gitignore",
    ".claude/**",
    ".agent-orch/**"
  ],
  "priority": 7,
  "status": "pending",
  "maxRetries": 2
}
