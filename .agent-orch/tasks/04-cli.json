{
  "id": "swatch-cli",
  "projectId": "swatch",
  "title": "CLI glue: main.c with arg parsing, env/TTY probing, parser→renderer wiring",
  "description": "Wire the parser and renderer into a CLI binary. This task DEPENDS on swatch-scaffold, swatch-parser, and swatch-renderer. It writes the only remaining C source file (src/main.c). The binary produced by `make` is the product.\n\nArgument parsing uses `getopt_long` (already in libc, no deps). Color mode is resolved at startup from env + isatty; the renderer itself does no probing.\n\nREADME.md already exists (authored at scaffold time) and documents the CLI surface — do NOT rewrite it, but if you add a flag or change behavior from what's documented there, update README.md to match. That is the only README edit permitted in this task.",
  "requirements": [
    "Implement `src/main.c`:\n  - `#include` parse.h, render.h, `<stdio.h>`, `<stdlib.h>`, `<string.h>`, `<unistd.h>`, `<getopt.h>`.\n  - `argv[0]` is the program name for usage strings; fall back to `\"swatch\"` if NULL.\n  - Accept exactly one positional argument: the hex string. If zero positional args, print usage to stderr and exit 64. If more than one, also exit 64.\n  - Long options via getopt_long:\n      * `--size WxH` — two integers separated by `x` (lowercase). Both must be >= 1 and <= 200 (sanity cap). Parse-failure exits 64.\n      * `--char CHAR` — exactly one character. More than one byte exits 64.\n      * `--label` — boolean flag, default off.\n      * `--help` — print usage (a few lines summarizing the flags and exit codes — keep it under 20 lines) and exit 0.\n      * `--version` — print `swatch 0.1.0\\n` and exit 0.\n  - Short options are NOT required in v0.1.0. Long-only is fine — pass `{0, 0, 0, 0}` for the short-option field where appropriate.\n  - After arg parsing, resolve color mode in this exact order:\n      1. If stdout is not a TTY (`isatty(fileno(stdout)) == 0`) → SWATCH_COLOR_NONE.\n      2. Else if `getenv(\"NO_COLOR\")` returns a non-NULL pointer (any value, including empty) → SWATCH_COLOR_NONE.\n      3. Else if `getenv(\"COLORTERM\")` returns a string containing the substring `\"truecolor\"` OR `\"24bit\"` (case-sensitive) → SWATCH_COLOR_TRUECOLOR.\n      4. Else → SWATCH_COLOR_XTERM256.\n  - Call `swatch_parse_hex`. On -1 return, print `swatch: malformed hex '<arg>'\\n` to stderr and exit 2.\n  - On success, populate swatch_render_opts_t and call `swatch_render(rgb, opts, stdout)`. Exit 0.\n  - Defaults: width=6, height=3, fill=' ', label=0. Do NOT hardcode the mode — take it from the env/TTY resolution above.",
    "Update README.md ONLY if the shipped CLI differs from what it documents. If it matches, do not touch README.md. (The scaffold committed a README. This task is allowed to edit it solely to correct drift, not to expand it.)",
    "Verify — run each and include the output in the report:\n  1. `make clean && make` — zero warnings, zero errors, produces ./swatch.\n  2. `make test` — both test binaries exit 0 under ASan/UBSan.\n  3. `./swatch --help` — exit 0, output mentions `--size`, `--char`, `--label`, `--version`.\n  4. `./swatch --version` — prints `swatch 0.1.0`, exit 0.\n  5. `NO_COLOR=1 ./swatch '#00AF4C'` — output is exactly `#00AF4C (6x3)\\n`, exit 0.\n  6. `NO_COLOR=1 ./swatch 00af4c` — identical output (case-normalized to uppercase in the NONE-mode report).\n  7. `./swatch 'nope' 2>&1` — exit 2, stderr contains `malformed hex 'nope'`.\n  8. `./swatch 2>&1` (no args) — exit 64, stderr shows usage.\n  9. `./swatch '#00AF4C' --size 2x1 --char '#' | xxd` — pipe is not a TTY → triggers NONE mode → hex dump shows ASCII `#00AF4C (2x1)\\n` regardless of flags. This proves the TTY check comes before flag application. Do NOT try to force truecolor in a pipe.\n  10. `make install PREFIX=/tmp/swatch-verify && test -x /tmp/swatch-verify/bin/swatch && rm -rf /tmp/swatch-verify` — install target works with a custom PREFIX and the installed binary is executable."
  ],
  "allowedPaths": [
    "src/main.c",
    "README.md"
  ],
  "forbiddenPaths": [
    "src/parse.h",
    "src/parse.c",
    "src/render.h",
    "src/render.c",
    "tests/test.h",
    "tests/test_parse.c",
    "tests/test_render.c",
    "Makefile",
    "idea.md",
    "CLAUDE.md",
    ".gitignore",
    ".claude/**",
    ".agent-orch/**"
  ],
  "priority": 5,
  "status": "blocked",
  "maxRetries": 2
}
